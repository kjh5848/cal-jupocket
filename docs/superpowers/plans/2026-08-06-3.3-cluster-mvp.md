# 프리랜서 3.3% 클러스터 MVP 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** cal.jupocket.com 의 플래그십 "프리랜서 3.3% 원천징수" 계산기를 계산로직·설명·PNG 공유까지 정적으로 만들어 배포한다.

**Architecture:** Astro + Cloudflare Workers Static Assets. 계산은 전부 클라이언트. 계산 로직은 `src/lib/` 순수 TypeScript 함수로 분리하고 vitest 단위 테스트로 숫자를 고정한다. 세율·상수는 `src/rates/<연도>.json`(국세청 대조). UI는 순수 Astro + 소량 vanilla JS.

**Tech Stack:** Astro 7, TypeScript, vitest, Cloudflare Wrangler, HTML canvas(PNG).

---

## 파일 구조 (책임 분리)

```
package.json            의존성·스크립트
astro.config.mjs        site=https://cal.jupocket.com, sitemap
tsconfig.json
wrangler.jsonc          정적 자산 배포(dist)
vitest.config.ts        단위 테스트
src/
  rates/2026.json       세율·상수(국세청 대조, 출처·확인일 메타)
  lib/
    money.ts            원 단위 반올림·포맷 (순수)
    withholding.ts      3.3%/8.8% 원천징수 계산 (순수)
    income-tax.ts       종합소득세 환급 예상 (순수)
    __tests__/          위 3개 단위 테스트
  components/
    BaseHead.astro      메타·canonical·OG·Clarity
    SiteHeader.astro    브랜드+내비
    SiteFooter.astro    링크+개인정보+고지
    AdSlot.astro        광고 슬롯(정책 배치)
    Explainer.astro     접이식 인라인 설명 블록
    Disclaimer.astro    "참고용" 고지
  lib/result-card.ts    결과→PNG canvas + 다운로드/공유/복사 (클라이언트)
  pages/
    index.astro         허브
    freelancer-33/index.astro    3.3% 원천징수 계산기(플래그십)
    privacy.astro
  styles/global.css
```

계산 로직(`lib/*.ts`)은 UI와 완전히 분리 — 입력→출력 순수함수, 테스트로 검증.

---

## Phase 1 — 기반 + 계산 로직 + 플래그십 계산기 + PNG + 배포

### Task 1: 프로젝트 스캐폴딩

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `vitest.config.ts`, `wrangler.jsonc`, `.gitignore`(이미 있음 — node_modules/dist 확인)

- [ ] **Step 1: package.json 생성**

```json
{
  "name": "cal-jupocket",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.0.0",
    "@astrojs/sitemap": "^3.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.5.0"
  }
}
```

- [ ] **Step 2: astro.config.mjs**

```js
// @ts-check
import { defineConfig } from "astro/config";
import sitemap from "@astrojs/sitemap";
export default defineConfig({
  site: "https://cal.jupocket.com",
  integrations: [sitemap()],
});
```

- [ ] **Step 3: tsconfig.json**

```json
{ "extends": "astro/tsconfigs/strict", "include": ["src", "*.mjs", "*.ts"] }
```

- [ ] **Step 4: vitest.config.ts**

```ts
import { defineConfig } from "vitest/config";
export default defineConfig({ test: { include: ["src/**/*.test.ts"] } });
```

- [ ] **Step 5: wrangler.jsonc**

```jsonc
{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "cal-jupocket",
  "compatibility_date": "2026-08-06",
  "assets": { "directory": "./dist", "not_found_handling": "404-page" }
}
```

- [ ] **Step 6: 설치·커밋**

Run: `npm install`
Expected: node_modules 생성, 오류 없음

```bash
git add package.json astro.config.mjs tsconfig.json vitest.config.ts wrangler.jsonc package-lock.json
git commit -m "chore: Astro+Cloudflare 스캐폴딩"
```

---

### Task 2: 세율·상수 검증 및 rates/2026.json

**Files:**
- Create: `src/rates/2026.json`

- [ ] **Step 1: 국세청 원문에서 값 확인 (사람 확인 항목)**

다음을 국세청/홈택스 원문에서 확인해 기록한다(미검증 값으로 채우지 않는다):
- 종합소득세 누진세율표(2026 과세연도): 과표 구간·세율·누진공제
- 지방소득세율(소득세의 10%), 사업소득 원천징수 3.3%(소득세 3%+지방세 0.3%)
- 기타소득 원천징수 8.8%(필요경비 60% 유형: 40%×22%)
- 단순경비율(주요 프리랜서 업종코드 몇 개), 인적공제 1인 150만, 표준세액공제 7만

- [ ] **Step 2: rates/2026.json 작성** (아래는 2023~2025 기준 구조 — Step 1에서 2026 확정값으로 교체)

```json
{
  "year": 2026,
  "verifiedOn": "2026-08-06",
  "source": "https://www.nts.go.kr/ (종합소득세 세율)",
  "withholding": { "business": 0.033, "other": 0.088 },
  "localTaxRate": 0.1,
  "incomeTaxBrackets": [
    { "upTo": 14000000, "rate": 0.06, "deduction": 0 },
    { "upTo": 50000000, "rate": 0.15, "deduction": 1260000 },
    { "upTo": 88000000, "rate": 0.24, "deduction": 5760000 },
    { "upTo": 150000000, "rate": 0.35, "deduction": 15440000 },
    { "upTo": 300000000, "rate": 0.38, "deduction": 19940000 },
    { "upTo": 500000000, "rate": 0.40, "deduction": 25940000 },
    { "upTo": 1000000000, "rate": 0.42, "deduction": 35940000 },
    { "upTo": null, "rate": 0.45, "deduction": 65940000 }
  ],
  "personalDeductionPerHead": 1500000,
  "standardTaxCredit": 70000,
  "simpleExpenseRateDefault": 0.6
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/rates/2026.json
git commit -m "data: 2026 세율·상수(국세청 대조)"
```

---

### Task 3: money.ts — 원 단위 반올림·포맷 (TDD)

**Files:**
- Create: `src/lib/money.ts`, `src/lib/__tests__/money.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

```ts
import { describe, it, expect } from "vitest";
import { won, formatWon } from "../money";

describe("won 반올림", () => {
  it("원 단위 반올림", () => {
    expect(won(1234.4)).toBe(1234);
    expect(won(1234.5)).toBe(1235);
  });
});
describe("formatWon", () => {
  it("천단위 콤마+원", () => {
    expect(formatWon(1234567)).toBe("1,234,567원");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- money`
Expected: FAIL (won/formatWon 미정의)

- [ ] **Step 3: 구현**

```ts
export function won(v: number): number {
  return Math.round(v);
}
export function formatWon(v: number): string {
  return won(v).toLocaleString("ko-KR") + "원";
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm test -- money`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/lib/money.ts src/lib/__tests__/money.test.ts
git commit -m "feat: money 반올림·포맷 유틸 + 테스트"
```

---

### Task 4: withholding.ts — 3.3%/8.8% 원천징수 (TDD)

**Files:**
- Create: `src/lib/withholding.ts`, `src/lib/__tests__/withholding.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, it, expect } from "vitest";
import { fromGross, fromNet, otherIncome } from "../withholding";

describe("사업소득 3.3%", () => {
  it("계약금액→원천징수·실수령", () => {
    const r = fromGross(1000000);
    expect(r.withholding).toBe(33000);
    expect(r.net).toBe(967000);
  });
  it("실수령→계약금액 역산", () => {
    expect(fromNet(967000).gross).toBe(1000000);
  });
});
describe("기타소득 8.8%", () => {
  it("총액→원천징수·실수령", () => {
    const r = otherIncome(1000000);
    expect(r.withholding).toBe(88000);
    expect(r.net).toBe(912000);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- withholding`
Expected: FAIL

- [ ] **Step 3: 구현**

```ts
import rates from "../rates/2026.json";
import { won } from "./money";

const B = rates.withholding.business; // 0.033
const O = rates.withholding.other;    // 0.088

export function fromGross(gross: number) {
  const withholding = won(gross * B);
  return { gross: won(gross), withholding, net: won(gross) - withholding };
}
export function fromNet(net: number) {
  const gross = won(net / (1 - B));
  const withholding = won(gross * B);
  return { gross, withholding, net: gross - withholding };
}
export function otherIncome(gross: number) {
  const withholding = won(gross * O);
  return { gross: won(gross), withholding, net: won(gross) - withholding };
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm test -- withholding`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/lib/withholding.ts src/lib/__tests__/withholding.test.ts
git commit -m "feat: 3.3%/8.8% 원천징수 계산 + 테스트"
```

---

### Task 5: income-tax.ts — 종소세 환급 예상 (TDD)

**Files:**
- Create: `src/lib/income-tax.ts`, `src/lib/__tests__/income-tax.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, it, expect } from "vitest";
import { estimateRefund } from "../income-tax";

describe("종소세 환급 예상", () => {
  it("총수입 3천만, 단순경비율 0.6, 본인1명", () => {
    // 소득금액 = 30,000,000*(1-0.6)=12,000,000
    // 과표 = 12,000,000 - 1,500,000(인적) = 10,500,000
    // 산출세액 = 10,500,000*0.06 - 0 = 630,000
    // 결정세액 = 630,000 - 70,000(표준) = 560,000
    // 기납부 = 30,000,000*0.033 = 990,000
    // 환급 = 990,000 - 560,000 = 430,000
    const r = estimateRefund({ grossIncome: 30000000, expenseRate: 0.6, dependents: 1 });
    expect(r.taxableBase).toBe(10500000);
    expect(r.computedTax).toBe(560000);
    expect(r.prepaid).toBe(990000);
    expect(r.refund).toBe(430000);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -- income-tax`
Expected: FAIL

- [ ] **Step 3: 구현**

```ts
import rates from "../rates/2026.json";
import { won } from "./money";

export interface RefundInput {
  grossIncome: number;   // 연간 사업소득 총수입
  expenseRate: number;   // 단순경비율 (0~1)
  dependents: number;    // 본인 포함 인적공제 인원
}

export function estimateRefund(input: RefundInput) {
  const income = input.grossIncome * (1 - input.expenseRate);
  const taxableBase = Math.max(
    0,
    income - input.dependents * rates.personalDeductionPerHead
  );
  const bracket = rates.incomeTaxBrackets.find(
    (b) => b.upTo === null || taxableBase <= b.upTo
  )!;
  const computedBefore = taxableBase * bracket.rate - bracket.deduction;
  const computedTax = Math.max(0, won(computedBefore) - rates.standardTaxCredit);
  const prepaid = won(input.grossIncome * rates.withholding.business);
  return {
    taxableBase: won(taxableBase),
    computedTax: won(computedTax),
    prepaid,
    refund: prepaid - won(computedTax),
  };
}
```

- [ ] **Step 4: 통과 확인**

Run: `npm test -- income-tax`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/lib/income-tax.ts src/lib/__tests__/income-tax.test.ts
git commit -m "feat: 종소세 환급 예상 계산 + 테스트"
```

---

### Task 6: 공용 레이아웃·컴포넌트

**Files:**
- Create: `src/components/BaseHead.astro`, `SiteHeader.astro`, `SiteFooter.astro`, `Disclaimer.astro`, `styles/global.css`

adsens-static 의 동일 컴포넌트 패턴을 따른다(브랜드=계산기 사이트, ClarityHead 별도 프로젝트 ID). 광고 없는 기본 레이아웃 먼저.

- [ ] **Step 1: styles/global.css** — 최소 읽기·폼 스타일(adsens-static global.css 축약: :root 변수, body, main measure, 입력/버튼, 표, 광고 슬롯, 접이식). (adsens-static/src/styles/global.css 참조해 이식)

- [ ] **Step 2: BaseHead.astro** — charset/viewport/title/description/canonical/OG + ClarityHead(프로젝트 ID는 발급 후).

- [ ] **Step 3: SiteHeader/SiteFooter/Disclaimer** — 브랜드+내비, 푸터(개인정보·고지), "참고용 예상치, 정확한 건 홈택스/세무사" 고지.

- [ ] **Step 4: 커밋**

```bash
git add src/components src/styles/global.css
git commit -m "feat: 공용 레이아웃·헤더·푸터·고지"
```

---

### Task 7: 플래그십 페이지 — 3.3% 원천징수 계산기

**Files:**
- Create: `src/pages/freelancer-33/index.astro`, `src/scripts/freelancer-33.ts`(클라이언트)

- [ ] **Step 1: 페이지 마크업** — 입력(계약금액 / 실수령 역산 토글), 결과 영역, 인라인 Explainer("3.3%는 소득세 3%+지방세 0.3%"), 관련 링크(종소세 환급·설명글), AdSlot(결과 아래), Disclaimer. `client:load` 대신 `<script>`로 lib 사용.

- [ ] **Step 2: 클라이언트 스크립트** — 입력 이벤트 → `fromGross`/`fromNet` 호출 → `formatWon`으로 결과 렌더. (lib는 정적 import — Astro가 번들)

```ts
import { fromGross, fromNet } from "../lib/withholding";
import { formatWon } from "../lib/money";
// DOM 바인딩: 입력값 파싱 → 계산 → 결과 텍스트 갱신
```

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`
Expected: dist 생성, 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add src/pages/freelancer-33 src/scripts/freelancer-33.ts
git commit -m "feat: 3.3% 원천징수 계산기 페이지"
```

---

### Task 8: 결과 PNG 카드 — 다운로드·공유·복사

**Files:**
- Create: `src/lib/result-card.ts`

- [ ] **Step 1: canvas 렌더 함수** — 입력·결과·"cal.jupocket.com"·고지를 canvas에 그려 dataURL 반환. 외부 라이브러리 없이 CanvasRenderingContext2D 직접 사용(시스템 폰트).

```ts
export function renderCard(data: { title: string; lines: [string, string][]; footer: string }): HTMLCanvasElement {
  const c = document.createElement("canvas");
  c.width = 1080; c.height = 1080;
  const g = c.getContext("2d")!;
  g.fillStyle = "#fff"; g.fillRect(0, 0, 1080, 1080);
  g.fillStyle = "#111"; g.font = "bold 56px sans-serif";
  g.fillText(data.title, 80, 140);
  g.font = "40px sans-serif"; let y = 300;
  for (const [k, v] of data.lines) {
    g.fillStyle = "#666"; g.fillText(k, 80, y);
    g.fillStyle = "#111"; g.textAlign = "right"; g.fillText(v, 1000, y);
    g.textAlign = "left"; y += 90;
  }
  g.fillStyle = "#0b5cad"; g.font = "32px sans-serif";
  g.fillText(data.footer, 80, 1000);
  return c;
}
export function downloadCard(canvas: HTMLCanvasElement, filename: string) {
  const a = document.createElement("a");
  a.href = canvas.toDataURL("image/png"); a.download = filename; a.click();
}
export async function shareCard(canvas: HTMLCanvasElement, text: string) {
  const blob: Blob = await new Promise((res) => canvas.toBlob((b) => res(b!), "image/png"));
  const file = new File([blob], "result.png", { type: "image/png" });
  if (navigator.canShare?.({ files: [file] })) {
    await navigator.share({ files: [file], text });
  } else {
    downloadCard(canvas, "result.png");
  }
}
```

- [ ] **Step 2: 페이지에 버튼 연결** — freelancer-33 결과에 "PNG 저장"·"공유"·"복사" 버튼 → `renderCard`+`downloadCard`/`shareCard`/clipboard. footer에 `cal.jupocket.com` 새김(유입 루프).

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add src/lib/result-card.ts src/pages/freelancer-33
git commit -m "feat: 결과 PNG 카드 다운로드·공유·복사"
```

---

### Task 9: index 허브 + 개인정보 + AdSense/Clarity

**Files:**
- Create: `src/pages/index.astro`, `src/pages/privacy.astro`, `src/components/AdSlot.astro`, `AdSenseHead.astro`, `ClarityHead.astro`

- [ ] **Step 1:** adsens-static 패턴으로 AdSenseHead/AdSlot/ClarityHead 이식(각 프로젝트 ID는 발급 후 채움). 개인정보방침(분석·광고·쿠키·고지) 작성.
- [ ] **Step 2:** index 허브 — 계산기·설명글 링크, 사이트 소개.
- [ ] **Step 3:** 빌드 확인 `npm run build`.
- [ ] **Step 4: 커밋**

```bash
git add src/pages/index.astro src/pages/privacy.astro src/components/AdSlot.astro src/components/AdSenseHead.astro src/components/ClarityHead.astro
git commit -m "feat: 허브·개인정보·광고·분석"
```

---

### Task 10: 배포 (cal.jupocket.com)

- [ ] **Step 1:** GitHub 원격 생성·push. Cloudflare Workers Builds 연결(또는 `wrangler deploy`). 커스텀 도메인 `cal.jupocket.com` 라우트 연결.
- [ ] **Step 2:** 라이브에서 계산·PNG·광고 로더·Clarity 확인(adsens-static 검증 스크립트 방식).
- [ ] **Step 3:** AdSense에 cal.jupocket.com 사이트 추가, GSC 속성 등록·사이트맵 제출.

---

## Phase 2 — 나머지 계산기 (Task 4/5 lib 재사용, Task 7/8 패턴 반복)

### Task 11: 원천징수 계산기(사업 3.3% + 기타 8.8% 토글) — `src/pages/withholding/index.astro`. lib는 Task 4 재사용. 페이지·PNG는 Task 7/8 패턴.

### Task 12: 종소세 환급 예상 계산기 — `src/pages/income-tax-refund/index.astro`. lib `estimateRefund`(Task 5) 사용. 입력(총수입·업종 단순경비율·인적공제 인원), 결과(환급/추납), 가정·고지 강조, "매년 받는 법" 체크리스트 인라인.

---

## Phase 3 — 설명 콘텐츠 (Google 유입·수익 엔진, §1.5·§1.6)

각 개념 글은 rates/2026.json 값을 인용(계산기와 동일 소스). 키워드 검증분부터.

### Task 13: "3.3% 환급이 나오는 구조" — `src/pages/guide/33-refund/index.astro`. 계산기로 CTA.
### Task 14: "종합소득세 세율·구간(2026)" — `src/pages/guide/income-tax-brackets/index.astro`. rates 표 렌더.
### Task 15: "단순경비율이란 / 기준경비율 차이" — `src/pages/guide/expense-rate/index.astro`.

(추가 개념 글은 GKP 검증 순으로.)

---

## 자체 검토

- **Spec 커버리지:** §1(계산기 3종)=Task 7/11/12. §1.5·§1.6(설명·플래닝)=Phase 3 + Explainer/체크리스트. §2(계산로직)=Task 3/4/5(테스트로 고정). §3(세율 버전)=Task 2. §4(아키텍처)=Task 1/6. §5(수익)=Task 9. §5.5(PNG)=Task 8. §6(고지)=Task 6 Disclaimer. §7(SEO)=sitemap+BaseHead. §8(갱신)=rates/<연도>.json 구조.
- **플레이스홀더:** 세율값은 Task 2 Step 1에서 국세청 대조 후 확정(의도된 검증 게이트). 그 외 코드 스텝은 완전한 코드 포함.
- **타입 일관성:** `won`/`formatWon`(money), `fromGross/fromNet/otherIncome`(withholding), `estimateRefund/RefundInput`(income-tax), `renderCard/downloadCard/shareCard`(result-card) — Phase 2/3에서 동일 시그니처 재사용.
